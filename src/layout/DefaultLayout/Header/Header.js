import React from 'react';
import { Text, View, SafeAreaView } from 'react-native';

const Header = () => {
    return (
        <View>
            <Text>Header</Text>
        </View>
    );
};

export default Header;
