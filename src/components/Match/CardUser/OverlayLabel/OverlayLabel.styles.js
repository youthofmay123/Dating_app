import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    overlayLabel: {
        justifyContent: 'center',
        alignItems: 'center',
        padding: 20,
        borderRadius: 50,
    },
});
